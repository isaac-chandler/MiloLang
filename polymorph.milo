#import "Standard"

main :: () {
	x: [..]s64
	
	array_add(*x, 4)
	array_add(*x, 13)
	array_add(*x, -5)
	
	for x {
		print("x[%] = %\n", it_index, it)
	}
	
	y: [..]f32
	
	array_add(*y, 3.2)
	
	for y {
		print("y[%] = %\n", it_index, it)
	}
}


